# ---- Core API ----
fastapi
uvicorn[standard]

# ---- Auth / Config ----
firebase-admin
python-dotenv
pydantic-settings
python-multipart
requests

# ---- ML / CV ----
numpy==1.26.4
opencv-python-headless==4.9.0.80

# ---- Face / Liveness ----
# mediapipe (commented to avoid build failure on Render free tier)

# ---- Torch (stable + compatible) ----
torch==2.1.2
torchvision==0.16.2
torchaudio==2.1.2

# ---- Audio Model ----
transformers
librosa